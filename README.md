# DHT11-Sensor
DHT11 sensor code to connect a microcontroller and the device. The code triggers the sensor, decodes its 40-bit data stream, validates the information against errors, and converts raw electrical pulses into readable temperature and humidity values for automation or displays.
